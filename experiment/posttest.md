#### Please attempt the following questions

<br>
Q1. In Naive Bayes classification, how do you handle continuous numerical features in the dataset?<br>
a. Discretize the features into discrete intervals <br>
b. Use Gaussian probability distribution for each feature <br>
c. Convert the features into binary values <br>
d. Ignore the continuous features and focus on categorical features <br>
<br>

Q2. How does Laplace smoothing (additive smoothing) help address the zero probability problem in Naive Bayes?<br>
a. It assigns a small non-zero probability to unseen events <br>
b. It assigns a zero probability to unseen events <br>
c. It increases the count of each event by 1 <br>
d. It reduces the count of each event by 1 <br>
<br>

Q3. When should you consider using Multinomial Naive Bayes over Gaussian Naive Bayes?<br>
a. When dealing with binary classification problems <br>
b. When the dataset contains continuous numerical features <br>
c. When the dataset contains categorical features <br>
d. When the dataset has imbalanced class distributions <br>
<br>

Q4. What is the purpose of feature selection in Naive Bayes?<br>
a. To reduce the dimensionality of the feature space <br>
b. To remove features that are not statistically significant <br>
c. To improve the performance of the Naive Bayes classifier <br>
d. To handle missing values in the dataset <br>
<br>

Q5. When is it appropriate to use Naive Bayes in real-world applications?<br>
a. When the dataset has a large number of features and a small number of instances <br>
b. When the class conditional independence assumption is violated <br>
c. When the dataset contains a mixture of continuous and categorical features <br>
d. When the class labels are highly correlated with each other <br>
<br>



